# coding=utf-8
# создайте базовое приложение на Flask. придумайте направление
# добавьте на свой сайт стили и действия
# Критерии оценки:
# + приложение Flask, blueprints и главный модуль разделены - 1 балл
# + применены стили - 1 балл
# + выполняются действия (например, при нажатии кнопки) - 1 балл

# веб-обёртка для скрипта, предназначенного для деплоя конфигурации на сетевые устройства
#

from app import app


if __name__ == '__main__':
    app.run(debug=True)
